# Crux-OS
Tarn is a Unix style operating system for the ESP32 S3. It runs a permanent kernel with a software MMU, demand paging, a persistent filesystem, MicroPython, SSH, and direct hardware control. Flash it once, then load your code at runtime. It turns a $5 chip into a real little computer.
